using Basic;

// Serching prime number 
PrimeNumber getPrime = new PrimeNumber();
       getPrime.PrintPrimeNumber(1,100);
       

        //Reverse string and Count in the strings
        /*string input = "This is my string";
        StringReverse stringReverse = new StringReverse();
        Console.WriteLine("The original string are:");
        Console.WriteLine(input);
        Console.WriteLine("The reverse string are:");
        stringReverse.StringReverseMethod(input);

        Console.WriteLine("The count string are:");

        Dictionary<char, int> characterCounts = stringReverse.CountStringsCharacters(input);
        foreach (var charCount in characterCounts)
        {
            Console.WriteLine("'" + charCount.Key + "': " + charCount.Value);
        }*/
 
 